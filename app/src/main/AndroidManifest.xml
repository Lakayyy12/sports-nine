<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.footballsbettips">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo25"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo25"
        android:supportsRtl="true"
        android:theme="@style/Theme.Earn_Money">

        <activity
            android:name="com.application.footballsbettips.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity
            android:name="com.application.footballsbettips.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>